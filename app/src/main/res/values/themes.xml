<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ShoppingApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange</item>

        <!-- Made it transparent so we can make status bar also follow the gradient color from toolbar , programmatically -->

        <item name="android:statusBarColor">@color/transparent</item>

        <item name="android:windowTranslucentStatus">true</item>

        <!-- To set status bar text color to black -->
        <item name="android:windowLightStatusBar">true</item>

        <item name="windowActionModeOverlay">true</item>

        <item name="actionModeStyle">@style/action_mode_style</item>


    </style>


    <style name="action_mode_style" parent="@style/Widget.Material3.ActionMode">
        <item name="background">@color/orange</item>
        <item name="titleTextStyle">@style/ActionModeTitleTextStyle</item>

    </style>

    <style name="ActionModeTitleTextStyle" parent="@style/TextAppearance.AppCompat.Widget.ActionMode.Title">


        <item name="fontFamily">@font/segeo_semi_bold</item>
    </style>

    <!-- Toolbar  titleText appearance -->
    <style name="ToolBarTitle">
        <item name="android:fontFamily">@font/segeo_bold</item>
        <item name="android:textColor">@color/black_gradient_1</item>
        <item name="android:textSize">18sp</item>

    </style>

    <style name="SpinnerTheme" parent="android:Widget.Spinner">
        <item name="android:background">@drawable/spinner_background</item>
    </style>

    <style name="loader_animation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>



    <style name="Theme.ShoppingApp" parent="Base.Theme.ShoppingApp" />


</resources>